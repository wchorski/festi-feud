{
  "compilerOptions": {
    "lib": [
      "ESNext",
      "DOM"
    ],
    "checkJs": true, // Enable type checking in JavaScript files
    "allowJs": true, // Allow JavaScript files (important for mixed TS/JS projects)
    "noEmit": true, // Prevents compilation (only type-checking)
    "module": "ESNext", // Supports modern ES modules
    "moduleResolution": "node", // Changed: Better for modern ES modules
    "target": "ES6", // Target modern JavaScript
    "baseUrl": ".",  // Add this
    "paths": { // Aliases (optional)
      "/*": [
        "."
      ],
      "@components/*": [
        "./components/*"
      ],
      "@types/*": [
        "./types/*"
      ]
    },
    "typeRoots": [
      "./node_modules/@types",
      "./types"
    ], // Ensure type definitions are used
    "strict": true, // Enable strict type checking
    "allowImportingTsExtensions": false, // Prevents .ts extensions in imports
    "resolveJsonModule": true, // Allow importing JSON files
    "esModuleInterop": true, // Better compatibility
    "forceConsistentCasingInFileNames": true // Catch casing errors
  },
  "include": [
    "**/*.js",
    "**/*.d.ts"
  ], // Include all JavaScript files in the `src` folder
  "exclude": [
    "node_modules",
    "**copy*.js",
    "assets"
  ] // Exclude `node_modules`
}